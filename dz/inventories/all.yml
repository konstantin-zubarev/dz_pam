all:
  children:
    web:
  vars:
    ansible_user: 'vagrant'

web:
  hosts:
    pam:
      ansible_host: 127.0.0.1
      ansible_port: 2222
      ansible_ssh_private_key_file: ./.vagrant/machines/pam/virtualbox/private_key
